These are boost libraries created from boost bcp.

I created cmake files for each boost library.

Right now it supports the following boost libraries:

Thread
Chrono
System
Program Options
FileSystem

More may be on the way


The header only libraries are also inclued:

Asio
Shared_ptr


...More not documented